syntax = "proto3";

package protocol;

import "typemanager.proto";
import "node.proto";

message State {
    TypeManager node_type_manager = 1;
    TypeManager pin_type_manager = 2;
    repeated Node nodes = 3;
}