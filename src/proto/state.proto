syntax = "proto3";

package protocol;

import "typemanager.proto";
import "node.proto";

message State {
    optional TypeManager node_type_manager = 1;
    optional TypeManager pin_type_manager = 2;
    repeated Node nodes = 3;
}